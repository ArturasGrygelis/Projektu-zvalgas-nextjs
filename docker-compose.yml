services:
  frontend:
    build:
      context: ./frontend
      target: development
    container_name: darbo_asistentas_frontend
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: npm run dev